# Backend API Configuration
# Copy this file to backend/.env and fill in your values

# AI Provider: stub (default) | openai | claude
# Default is "stub" - free, fast, no API costs
AI_PROVIDER=stub

# Only needed if AI_PROVIDER=openai
OPENAI_API_KEY=

# Only needed if AI_PROVIDER=claude
ANTHROPIC_API_KEY=

# Server port (default: 3001)
PORT=3001
